<div>
{{#if showAmount}}
<h1 class="title"> Shows Found </h1>
<h2 class="searchTerm"> Search: {{searchTerm}} </h2>
    <div>
        <ul> 
            {{#each shows}}
                <li> <a href = "/shows/{{show.id}}" class="name"> {{show.name}} </a> </li>
            {{/each}}
        </ul>
    </div>
{{else}}
<p>Apologies, We are not finding this show, no results found for {{searchTerm}}.</p>
{{/if}}
<a class="diffSearch" href="/">Try a different search</a>
</div>